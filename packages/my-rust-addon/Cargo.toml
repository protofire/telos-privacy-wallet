[package]
name = "my-rust-addon"
version = "1.0.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
neon = { version = "1", default-features = false, features = ["napi-8"] }

[profile.release]
lto = true
